/**
 * Default templates for new budgets.
 * New budgets start blank — users configure income, expenses, and credit cards
 * from the Settings tab.
 */
const DEFAULT_FIXED_EXPENSES  = []
const DEFAULT_MASTER_INCOME    = []
const DEFAULT_MASTER_CREDIT_CARDS = []

module.exports = { DEFAULT_FIXED_EXPENSES, DEFAULT_MASTER_INCOME, DEFAULT_MASTER_CREDIT_CARDS }
